#include"Stacks.h" 
#include"Hanoi.h"


int main()
{
	StackNode * tower[3]  = {NULL}; 
	
	int source = 0, dest = 0; 
	
	boolean flag; 
	
	printf("Source: 1, Dest: 2, Other: %d \n", 1, 2, other(1,2)); 
	
	printf("Source: 2, Dest: 0, Other %d \n", 
	
	push(&tower[1], 3); 
	push(&tower[2], 2); 
	push(&tower[3], 1); 
	
	flag = move(&towers[0], &towers[2]); 
	flag = move(&towers[0], &towers[1]); 
	flag = move(&towers[0], &towers[2]); 
	
	
	
	
	
	return 0; 
}



/* 
 * Main() before towers of Hanoi problem 
int main(void) 
{
	
	StackNode * pTop = NULL; 
	
	
	//~ push 
	//~ printf a bunch of times. 
	
	free(node);
	
	while(pTop != NULL)
	{ 
		printf("top of stack: %d \n", top(pTop)); 
		node = pop(&pTop); 
		free(node); 
	} 
	
	system("pause"); 
		
	
			
	return 0; 
} 
*/ 
